apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  labels:
    app: replication 
  name: app-source-placement-0
  namespace: dokuwiki
spec:
  clusterSelector:
    matchExpressions: []
    matchLabels:
      site: headquarters 

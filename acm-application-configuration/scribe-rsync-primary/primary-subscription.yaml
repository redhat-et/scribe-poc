apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: primary-subscription
  namespace: scribe 
  labels:
    name: primary
  annotations:
    apps.open-cluster-management.io/github-path: acm-application-configuration/primary-scribe-configuration 
    apps.open-cluster-management.io/github-branch: main
spec:
  channel: scribe/store
  placement:
    placementRef:
      kind: PlacementRule
      name: primary-cluster 

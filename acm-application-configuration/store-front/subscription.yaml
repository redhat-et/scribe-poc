apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: store
  namespace: scribe
  labels:
    name: store
  annotations:
    apps.open-cluster-management.io/github-path: acm-application-configuration/application
    apps.open-cluster-management.io/github-branch: main
spec:
  channel: scribe/store
  placement:
    placementRef:
      kind: PlacementRule
      name: store
